name: generate badge
on:
  workflow_dispatch:
  push:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  update:
    name: Update stats card
    runs-on: ubuntu-latest
    steps:
      - uses: datarootsio/github-stats-card@v1
        with:
          username: moddroid94
          gh_token_stats: ${{ secrets.PAT_TOKEN }}
          gh_token_commits: ${{ secrets.PAT_TOKEN }}
          badge_path: assets/badge.svg
          header: "👋 hi i'm"
          about: "A passionate Thinkerer from Italy.\n Mostly self-taught developer and EE"
          theme: terminal-green
          commit_message: Badge built by Oompa-Loompas
